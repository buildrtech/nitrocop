while cond
end

until cond
end

while x.any?
  do_something(x.pop)
end

while total_width >
      minimum_width
  pack_column
end

while (
  needs_more &&
  still_running
)
  buf = stack.pop
end

while !found_assoc &&
      segments.size > 0
  find_assoc
end

until versions.length == 0 ||
      versions.shift <= previous_version
  history_string += version_lines.shift
end

while !block.empty? &&
      react_wunderbar_free([block.first])
  prolog << process(block.shift)
end

while iter # Keep spinning until told otherwise
  iter += 1
end

until i > 9 i += 1
end

while i < 3 i += 1
end
